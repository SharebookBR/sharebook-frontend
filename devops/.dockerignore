# .git é usado propositalmente pelo build (scripts/generate-build-info.js lê
# .git/HEAD pra gravar o commit no rodapé) — não ignorar. Como o build é
# multi-stage, .git nunca chega na imagem final de runtime.
node_modules
.gitignore
.angular
.claude
.netlify

Dockerfile*
README*
dist
